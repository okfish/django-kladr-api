============
Installation
============

At the command line::

    $ easy_install django-kladr-api

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-kladr-api
    $ pip install django-kladr-api
